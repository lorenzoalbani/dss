SELECT 
    {[Measures].[Streams 1month]} ON 0,

    NON EMPTY 
    (
        [Dim Time].[Month_flat].[Month_flat].Members
        * FILTER(
            DESCENDANTS(
                [Dim Artist].[Geography].[Country], 
                [Dim Artist].[Geography].[Region], 
                SELF_AND_BEFORE
            ),
            [Dim Artist].[Geography].CurrentMember.Name <> "Unknown" 
        )
    ) ON 1

FROM [Group ID 3 DB]